# Aethelgrad Game TODO

- [x] Define the original cinematic fantasy login composition and online-only interaction rules
- [x] Replace the current onboarding layout with an original title crest, world vignette, region selector, account-status panel, and settings entry
- [x] Preserve the secure Google Play sign-in, mandatory server readiness, consent gate, and explicit authentication-state handling
- [x] Keep guest mode unavailable in release builds and avoid copying supplied reference artwork, logo, or text
- [x] Validate the redesigned Android APK through public CI and publish the milestone artifact
- [x] Calculate the current Android debug signing SHA-1 and SHA-256 for Google OAuth registration
- [x] Create an original, legally clean Aethelgrad cinematic login background asset without using the supplied reference image
- [x] Integrate the original background asset into the Android login experience with a procedural fallback and dark UI-safe vignette
- [x] Validate the packaged background asset in the Android release build and public CI
- [x] Add a standard Google Sign-In backend contract for testing without Play Console, while preserving the future Play Games path
- [x] Implement backend ID-token verification, game-account upsert, and secure rotating Aethelgrad sessions
- [x] Replace the Android Play Games-only login attempt with a standard Google Sign-In token exchange flow
- [x] Document the required Android and Web OAuth clients, backend secrets, deployment, and Play Games upgrade path
- [x] Validate the standard Google Sign-In backend and Android release build through automated tests and CI
- [x] Provision a managed HTTPS standard-Google-sign-in development backend with database-backed Aethelgrad sessions
- [ ] Publish the managed backend on a stable production HTTPS domain before public release
- [x] Inspect the Drive error evidence for the authenticated login flow that fails to advance
- [x] Repair the post-authentication transition so the login page is sign-in only and authenticated players reach the next setup page
- [x] Redesign the sign-in-only page with original Aethelgrad artwork and layout informed only by the supplied reference’s high-level mood
- [x] Connect authenticated Android characters to account-bound cloud-world manifest recovery and profile-avatar selection
- [x] Implement client-side versioned cloud-save upload/download handling without storing session tokens on disk
- [x] Render selectable built-in profile avatars and username identity in the authenticated character setup flow
- [x] Present recovered cloud worlds with explicit resume and conflict-recovery states on Android
- [x] Add native snapshot round-trip validation for the cloud-state export/import bridge
- [x] Connect the Android login configuration to the managed backend after the Web OAuth client ID is created
- [x] Surface actionable Android Credential Manager failure diagnostics instead of a generic cancelled-or-unavailable message.
- [x] Verify the release signing certificate fingerprint and Google Android OAuth registration required for device Google Sign-In.
- [x] Diagnose the reported Google Credential Manager cancellation before backend exchange and improve the device-facing remediation guidance.
- [x] Separate the offline `.prototype` harness from the online release at launch and document the matching Google OAuth package/artifact path; physical release-device sign-in validation remains required.
- [x] Update the Android release authentication base URL and exchange/refresh endpoints to the live Render v2 `/v1/auth/google-id-token/exchange` and `/v1/auth/refresh` contract, then verify the real Google token exchange.
- [x] Verify the downloaded release APK embeds the corrected live game-auth host and diagnose any remaining post-Google exchange failure without relying on stale build artifacts.
- [x] Restore and lock the required Android namespace and application ID `com.darkvirgoyt.aethelgrad`, including matching Kotlin declarations, JNI exports, CI checks, and Google OAuth registration.
- [x] Add authenticated logout and character/inventory access controls, and repair reversed horizontal joystick direction in the mobile gameplay HUD.
- [x] Add a centered aim crosshair, repair jump activation, replace the sprint box with a touch-first hold control, move the profile to the top-right, and add a circular top-left mini-map.
- [x] Add a premium top-right health bar and stamina meter beside the profile icon, driven by the existing live native HUD state.
- [x] Design and implement the Emberling companion-bond milestone with original creature interaction, visible follow/stay behavior, cloud-state persistence, native regression coverage, and explicit mobile/online authority boundaries.
- [x] Confirm on a physical Android device that Google sign-in exchanges successfully with the live backend and opens the authenticated world/profile flow.
- [x] Diagnose and repair the post-login high-graphics content download timeout that locks authenticated players out of world entry; preserve release integrity checks while permitting verified core online-world entry only when the optional high-resolution service is unavailable.
- [ ] Diagnose the renewed Google sign-in internal error against the exact installed APK certificate SHA-1, locked package identity, and live backend exchange route; rebuild and retest the matching release artifact.
- [x] Create a separate original Unreal Engine C++ production foundation under `Unreal/`, preserving the Kotlin/OpenGL Android build as the active harness and excluding Unreal Engine source and third-party protected assets from the repository; add a first-playable session coordinator, milestone contract, and asset-governance record. Unreal compilation and Android cooking remain pending on an Unreal-capable environment.
- [x] Add a legally safe Unreal 3D terrain workflow that treats the selected Google Earth polygon as a planning boundary only, requires independently licensed elevation data, and generates original playable biomes, landmarks, and asset placement. Actual polygon export, DEM processing, Unreal Landscape import, authored assets, and device profiling remain pending.
- [x] Use `Untitledmapproject.kml` and `forest.kml` only as private layout references to create an original Aethelgard 3D world plan; do not import or reconstruct Google imagery, terrain, roads, landmarks, or elevation output. Added the KML exclusion policy, a seed-driven fictional six-biome atlas, and the Verdant Veil first-playable world contract; Unreal compilation and authored asset creation remain pending.
- [x] Add a server-authoritative Unreal creature-spawning foundation for original animals and hostile mobs, including biome profiles, deterministic placement candidates, spawn budgets, distance safety, encounter gating, and original creature class hooks. Unreal Blueprint creature assets, behavior trees, multiplayer playtests, and Android performance validation remain pending.
- [x] Create an Unreal Blueprint implementation guide for the original `BP_RootbackGrazer` and `BP_DuskmawProwler` creature classes, covering components, spawn profiles, authoritative event handling, AI hooks, and validation steps. Blueprint assets, behavior trees, licensed/original animal assets, runtime testing, and device profiling remain pending in a UE 5.6+ environment.
- [x] Align the base wild-creature class with Unreal navigation and AI-controller ownership so the two Blueprint creature classes can run behavior-tree and perception setup. Unreal compilation and editor validation remain pending.
- [x] Verify and document Rootback Grazer and Duskmaw Prowler axis movement, walk/run/sprint speeds, braking, rotation, jump length, and multiplayer authority boundaries. Unreal editor execution, navmesh traversal, pounce implementation, replication behavior, and Android performance tests remain pending.
- [x] Repair the unavailable high-graphics resource path: inventory real cooked/map/model content, reject unconfigured or unverifiable archives, show exact available byte size and actual 0–100% verified progress only, and clearly distinguish optional Android content from uncooked Unreal source/assets. Publishing original/licensed cooked Unreal content, configuring a signed archive or Play Asset Delivery release, physical-device validation, and performance profiling remain pending.
- [x] Add a concise immutable release-configuration README and validator that lock Android package/JNI identity, managed game-auth endpoints, online-only policy, release content publication gate, and private engine-source boundary against accidental edits. Any future intentional change must update the lock, matching source/CI/hosting configuration, and real-device validation together.
- [x] Raise the downloadable Android release minimum to Android 11 (API 30), validate the Android 11+ APK contract in CI, and preserve the truthful unpublished high-graphics content gate. A physical Android 11+ install/download test and real cooked-content publication remain pending.
- [x] Upgrade the Android native OpenGL renderer with real, bounded visual improvements and an asset-ready graphics path, while keeping the absence of real cooked Unreal maps/models explicit until original/licensed content is produced. Original/licensed high-resolution art production, UE Android cooking, device GPU profiling, and signed content publication remain pending.
- [x] Diagnose and repair the Android Google sign-in “Internal error” path by verifying the installed APK identity, Google credential failure handling, managed exchange endpoint, and user-visible actionable diagnostics without exposing account or token data. Physical-device confirmation with the newly built APK and the exact signing certificate registered in Android OAuth remain pending.
- [x] Make Google account-chooser cancellation recoverable: reliably clear in-flight login state, re-enable the sign-in action, and provide a clear retry instruction without bypassing Android OAuth configuration checks. Physical-device confirmation remains required with the latest CI APK and the exact Android OAuth signing SHA-1.
- [x] Prevent duplicate Aethelgard launcher installs by enforcing the one locked Android package identity, documenting the one-time cleanup of earlier wrong-package APKs, and adding a concise CI-checked migration marker. The user must uninstall the already-installed historical wrong-package app once because Android updates cannot remove a different package.
- [x] Add an explicit required account-link consent checkbox and identity-only cloud-save explanation to the Google login screen, without requesting unnecessary Gmail or Drive permissions, and keep cancellation feedback accurate. Physical-device confirmation with the updated consent screen and successful post-selection backend exchange remains pending.
- [x] Diagnose and correct the remaining post-account-selection credential callback by switching to Credential Manager’s standard all-account Google ID-token option, keeping true cancellation separate from provider/token/backend errors, and preserving identity-only scopes. The next CI APK and physical device test must confirm a completed selected-account exchange.
- [ ] Resolve the Android `INSTALL_FAILED_DUPLICATE_PACKAGE` pending-install queue for the locked `com.darvirgoyt.aethelgrad` APK, then verify the current installed build reaches the Google account chooser and reports the exact next safe credential state.
- [ ] Confirm the Realme C30 Android version and recover its package-installer queue without uninstalling the app unless the queue remains blocked after a restart and a direct extracted-APK install.
- [ ] Perform and verify the final Realme C30 clean reinstall of `com.darvirgoyt.aethelgrad` after the cache-only recovery did not clear the pending duplicate package session.
- [ ] Verify the current Android 17 device installs the latest successful CI APK and returns from a selected Google account into the authenticated Aethelgard account/world flow; keep the legacy Realme C30 installer queue isolated from this test.
- [x] Verify whether any release-contract change is actually required for Android 14-class device compatibility, retaining Android 11 minimum support unless evidence proves an Android 14-specific packaging issue. The release already supports Android 11+ (`minSdk 30`) and targets API 35, so no Android 14-specific source change is needed.
- [x] Polish the Android game presentation with a more cohesive high-end settings treatment, add truthful device-aware performance profiles, and document that OEM Game Space enrollment remains user-controlled rather than app-forced. Profiles never exceed the detected display mode or claim unpublished high-end content.
- [x] Add an explicit guest local-play mode with device-local save storage, visible no-cloud/no-co-op limits, and a separate Google account-link upgrade path that does not misrepresent guest data as account-restorable. Guest mode is validated to avoid backend/token requests and to block hosted co-op.
- [x] Repair player movement and jump input, replace visibly random creature motion with deterministic steering, reduce and align the mobile HUD controls, shift the crosshair slightly right for aiming, smooth the original world map, and keep the control design original rather than copying the supplied reference artwork or exact UI. Static HUD/input contracts plus native physics/controller tests pass; physical touch testing remains required.
- [ ] Connect an Unreal Engine-capable desktop or GPU cloud desktop, inspect its UE/Android toolchain and Aethelgard project folder, then build and validate only the targets the connected machine can support.
- [x] Redesign the active gameplay HUD to remove debug/status clutter, establish an original profile-and-region hierarchy, compact and group action controls, preserve the quick inventory, and improve visual coherence without copying Ark, Palworld, or the supplied reference UI. Static contracts pass; a physical-device visual review remains required.
- [x] Evaluate GDevelop as a separate Aethelgard UI/prototype workflow, document its real Android/3D limits, and avoid replacing the existing Kotlin/OpenGL or Unreal production path without an explicit approved migration plan. GDevelop is suitable for a fresh, original prototype or UI experiment, not a drop-in Unreal replacement.
- [ ] Maintain Unreal Engine as Aethelgard’s exclusive long-term graphics and production path; do not begin a GDevelop migration or parallel GDevelop prototype unless the user explicitly changes this decision.
- [x] Inspect and repair the `assetpack_cinematics` Android asset-pack module, including its delivery configuration, module registration, runtime references, and truthful statement of whether original cinematic payloads are actually present.
- [x] Inspect and repair the `assetpack_cinematics` Android asset-pack module, including its delivery configuration, module registration, runtime references, and truthful statement of whether original cinematic payloads are actually present. It remains correctly registered as on-demand but is explicitly zero-byte/unpublished and cannot be requested through high-tier sectors before a signed cooked release exists.
- [x] Audit every currently failing GitHub Actions run and production-foundation check, distinguish immutable historical failures from the current main branch status, repair any reproducible source failure, and verify the newest Android workflow is green.
- [x] Correct Android mobile input semantics: make a smaller left-side virtual joystick stay anchored to its configured touch zone, prevent any right-side look/camera input from being horizontally inverted, and add regression checks for movement, look, multi-touch separation, and safe placement.
- [ ] Perform physical-device follow-up for the revised joystick, look camera, control placement, and current release APK after CI validation; automated checks cannot confirm touch feel or device-specific layout.
- [x] Inspect and repair `assetpack_core` registration, delivery mode, packaged-content boundary, manifest/runtime references, and budget-contract coverage; verify the current Android workflow stays green without inventing core payload size.
- [x] Verify the exact truncated `assetpack_core` workflow log reported by the user, identify its post-resource-center result, and reconcile it with the current successful main-branch workflow.
- [x] Verify and clarify the `assetpack_core` GitHub folder-header status so the current passing commit is distinguishable from the historical failed `b584b81` commit shown in the user’s screenshot.
- [x] Verify the current Android joystick is smaller, fixed to the lower-left anchor, and direct/non-reversed in both movement and camera look paths; retain physical-device touch validation as a separate requirement.
- [x] Update and verify the app-side content contract so the GitHub `app` folder header points to a current passing commit instead of the historical failed `b584b81` commit.
- [x] Update and verify the truthful zero-byte `assetpack_dungeons` deferred-pack state so its GitHub folder header points to a current passing commit instead of the historical failed `a744758` commit.
- [x] Update and verify the truthful zero-byte `assetpack_hd_textures` deferred-pack state so its GitHub folder header points to a current passing commit instead of the historical failed `a744758` commit.
- [x] Update and verify the truthful zero-byte `assetpack_pipeline_cache` deferred-pack state so its GitHub folder header points to a current passing commit instead of the historical failed `a744758` commit.
- [x] Verify the current GitHub folder commits and workflow outcomes for `assetpack_dungeons`, `assetpack_hd_textures`, and `assetpack_pipeline_cache`, distinguishing the user’s historical screenshots from the current main branch.
- [x] Update and verify the truthful zero-byte `assetpack_shaders_vulkan` deferred-pack state so its GitHub folder header points to a current passing commit instead of the historical failed `a744758` commit.
- [ ] Audit every tracked repository folder whose latest GitHub-visible commit still carries a historical failed status, repair all remaining truthful module-state or validation gaps in one coordinated change, and verify the current main workflow is green.
